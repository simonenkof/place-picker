# Place Picker

Сервис для бронирования рабочих мест в опенспейсе. Позволяет пользователям просматривать доступные рабочие места, выбирать дату и время, а также управлять своими бронированиями.

## 📋 Содержание

- [Описание](#описание)
- [Архитектура](#архитектура)
- [Технологический стек](#технологический-стек)
- [Требования](#требования)
- [Установка и настройка](#установка-и-настройка)
- [Конфигурация](#конфигурация)
- [Развертывание через Docker Compose](#развертывание-через-docker-compose)
- [Запуск проекта](#запуск-проекта)
- [Сборка проекта](#сборка-проекта)
- [API документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Разработка](#разработка)

## Описание

Place Picker — это веб-приложение для управления бронированием рабочих мест в опенспейсе. Система позволяет:

- Регистрацию и аутентификацию пользователей
- Просмотр доступных рабочих мест
- Бронирование мест на определенное время
- Управление своими бронированиями
- Автоматическую очистку устаревших бронирований

## Архитектура

Проект представляет собой монорепозиторий, состоящий из двух основных приложений:

- **Backend** (`apps/back`) — REST API сервер на Go (Gin framework)
- **Frontend** (`apps/front`) — SPA приложение на Angular 19

Backend обслуживает API запросы и в production режиме также раздает статические файлы фронтенда. В режиме разработки фронтенд работает отдельно на своем порту.

## Технологический стек

### Backend

- **Go 1.24+**
- **Gin** — сервер
- **PostgreSQL** — база данных
- **golang-migrate** — миграции БД
- **JWT** — аутентификация
- **Viper** — управление конфигурацией
- **Gomail** — отправка email

### Frontend

- **Angular 19** — фреймворк
- **Taiga UI** — UI библиотека компонентов

### Инфраструктура

- **Nx** — монорепозиторий и система сборки
- **Docker Compose** — контейнеризация всего стека (БД, Backend, Frontend)

## Требования

- **Node.js** 18+ и npm
- **Go** 1.24+
- **Docker** и **Docker Compose** (для развертывания через Docker Compose)
- **PostgreSQL** 15+ (или Docker для запуска через docker-compose)
- **Make** (опционально, для некоторых команд)

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd place-picker
```

### 2. Установка зависимостей

```bash
# Установка Node.js зависимостей
npm install

# Установка Go зависимостей (выполнится автоматически при сборке)
cd apps/back
go mod download
cd ../..
```

### 3. Настройка базы данных

#### Вариант 1: Развертывание через Docker Compose (рекомендуется)

В корне проекта есть `docker-compose.yml` для полного развертывания стека. См. раздел [Развертывание через Docker Compose](#развертывание-через-docker-compose).

#### Вариант 2: Только база данных через Docker Compose

Запустите только PostgreSQL через Docker Compose:

```bash
cd apps/back
docker-compose up -d
```

Это создаст:

- PostgreSQL на порту `5432`
- pgAdmin на порту `8080` (опционально, для управления БД)

#### Вариант 3: Использование существующей PostgreSQL

Или используйте существующую PostgreSQL базу данных.

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта `apps/back/` со следующим содержимым:

```env
# База данных
PLACE_PICKER_DB_HOST=localhost
PLACE_PICKER_DB_PORT=5432
PLACE_PICKER_DB_USER=admin
PLACE_PICKER_DB_PASSWORD=admin
PLACE_PICKER_DB_NAME=place-picker
PLACE_PICKER_DB_SSLMODE=disable

# JWT
PLACE_PICKER_JWT_SECRET=your-secret-key-here-change-in-production

# Email (опционально, для верификации пользователей)
PLACE_PICKER_SMTP_PROVIDER=smtp.example.com
PLACE_PICKER_SMTP_PORT=587
PLACE_PICKER_MAIL_USER=your-email@example.com
PLACE_PICKER_MAIL_PASSWORD=your-password
PLACE_PICKER_DOMAIN=http://localhost:3276
```

**Важно:** Измените `PLACE_PICKER_JWT_SECRET` на безопасный случайный ключ в production!

## Конфигурация

### Backend конфигурация

Создайте файл `config.yaml` в директории `apps/back/`:

```yaml
mode: 'dev' # dev, prod, test
logs_path: './logs/proxy_server.log'
frontend_path: '../../dist/apps/front/browser'
dev_frontend_url: 'http://localhost:4202'
http_server:
  port: ':3276' # Порт должен начинаться с ":"
```

**Режимы работы:**

- `dev` — режим разработки, фронтенд проксируется на отдельный порт
- `prod` — production режим, фронтенд раздается статически
- `test` — режим тестирования

### Frontend конфигурация

Настройки окружения находятся в `apps/front/src/app/enviroments/`:

- `environment.ts` — для разработки
- `environment.prod.ts` — для production

## Развертывание через Docker Compose

Полное развертывание всего стека (PostgreSQL, Backend, Frontend) через Docker Compose.

### Требования

- **Docker** и **Docker Compose**
- Собранные файлы должны находиться в `dist/apps/back` и `dist/apps/front`

### Подготовка

1. Соберите фронтенд:

```bash
npm run build:f
```

2. Соберите бэкенд для Linux:

```bash
npm run build:b
```

Команда `build:b` автоматически собирает бинарник для Linux (GOOS=linux GOARCH=amd64).

3. Убедитесь, что файл `config.yaml` находится в `dist/apps/back/`:

```bash
cp apps/back/config.yaml dist/apps/back/
```

Или создайте его вручную:

```yaml
mode: 'prod'
logs_path: './logs/proxy_server.log'
frontend_path: './front'
http_server:
  port: ':3276'
```

### Настройка переменных окружения

Отредактируйте `docker-compose.yml` в корне проекта и настройте переменные окружения для бэкенда:

```yaml
environment:
  # База данных
  PLACE_PICKER_DB_HOST: postgres
  PLACE_PICKER_DB_PORT: 5432
  PLACE_PICKER_DB_USER: admin
  PLACE_PICKER_DB_PASSWORD: admin
  PLACE_PICKER_DB_NAME: place-picker
  PLACE_PICKER_DB_SSLMODE: disable

  # JWT Secret (обязательно измените в production!)
  PLACE_PICKER_JWT_SECRET: your-secret-key-here-change-in-production

  # Email конфигурация (опционально)
  PLACE_PICKER_SMTP_PROVIDER: smtp.example.com
  PLACE_PICKER_SMTP_PORT: 587
  PLACE_PICKER_MAIL_USER: your-email@example.com
  PLACE_PICKER_MAIL_PASSWORD: your-password
  PLACE_PICKER_DOMAIN: http://localhost:3276
```

**Важно:** Измените `PLACE_PICKER_JWT_SECRET` на безопасный случайный ключ!

### Запуск

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Просмотр логов только бэкенда
docker-compose logs -f backend

# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением volumes (удалит данные БД!)
docker-compose down -v
```

### Доступ к сервисам

После запуска доступны:

- **Backend API**: http://localhost:3276
- **Frontend**: http://localhost:3276 (обслуживается бэкендом)
- **PostgreSQL**: localhost:5432
- **pgAdmin**: http://localhost:8080
  - Email: `admin@example.com`
  - Password: `admin`

## Запуск проекта

### Режим разработки

Запуск всех сервисов одновременно:

```bash
npm run start:all
```

Или по отдельности:

```bash
# Запуск только фронтенда
npm run start:f

# Запуск только бэкенда
npm run start:b
```

После запуска:

- **Frontend**: http://localhost:4202
- **Backend API**: http://localhost:3276

### Production режим

#### Вариант 1: Через Docker Compose (рекомендуется)

См. раздел [Развертывание через Docker Compose](#развертывание-через-docker-compose).

#### Вариант 2: Локальный запуск

1. Соберите фронтенд:

```bash
npm run build:f
```

2. Настройте `config.yaml` для production режима:

```yaml
mode: 'prod'
frontend_path: '../../dist/apps/front/browser'
```

3. Соберите и запустите бэкенд:

```bash
cd apps/back
go build -o server main.go
./server
```

Backend будет обслуживать и API, и фронтенд на порту, указанном в конфигурации.

## Сборка проекта

### Сборка всех приложений

```bash
npm run build:all
```

### Сборка по отдельности

```bash
# Сборка фронтенда
npm run build:f

# Сборка бэкенда (для Linux, для Docker)
npm run build:b

# Сборка бэкенда для текущей платформы (для локального запуска)
cd apps/back
go build -o server main.go
```

Собранные файлы будут находиться в директории `dist/`.

**Важно:** Команда `npm run build:b` собирает бинарник для Linux (GOOS=linux GOARCH=amd64), что необходимо для запуска в Docker контейнере. Для локального запуска на macOS используйте обычную команду `go build`.

## API документация

API документация находится в `apps/back/docs/api/`. Для просмотра:

1. Откройте `apps/back/docs/api.html` в браузере

Или соберите документацию самостоятельно:

```bash
cd apps/back
make build-api
```

Требуется установленный пакет `@redocly/cli`:

```bash
npm install -g @redocly/cli
```

### Основные эндпоинты

- `POST /api/auth/register` — регистрация пользователя
- `POST /api/auth/login` — авторизация
- `POST /api/auth/refresh` — обновление токена
- `GET /api/private/desks` — получение списка рабочих мест
- `GET /api/private/reservation` — получение бронирований
- `POST /api/private/reservation` — создание бронирования
- `DELETE /api/private/reservation/:id` — удаление бронирования
- `GET /api/private/user/me` — информация о текущем пользователе

Все приватные эндпоинты требуют JWT токен в заголовке `Authorization: Bearer <token>`.

## Структура проекта

```
place-picker/
├── apps/
│   ├── back/                 # Backend приложение
│   │   ├── internal/
│   │   │   ├── api/          # API handlers
│   │   │   ├── config/       # Конфигурация
│   │   │   ├── db/           # Работа с БД
│   │   │   ├── jwt/          # JWT токены
│   │   │   ├── logger/       # Логирование
│   │   │   ├── mail/         # Email сервис
│   │   │   └── server/       # HTTP сервер
│   │   ├── migrations/       # Миграции БД
│   │   ├── docs/             # API документация
│   │   ├── config.yaml       # Конфигурация сервера
│   │   └── main.go           # Точка входа
│   │
│   └── front/                # Frontend приложение
│       └── src/
│           ├── app/
│           │   ├── components/  # UI компоненты
│           │   ├── modules/     # Модули приложения
│           │   ├── services/    # Сервисы
│           │   └── models/      # Модели данных
│           └── assets/          # Статические ресурсы
│
├── dist/                     # Собранные файлы
├── package.json              # Node.js зависимости
└── nx.json                   # Nx конфигурация
```

## Разработка

### Тестирование

Запуск тестов фронтенда:

```bash
npm run test:f
```

### Миграции базы данных

Миграции применяются автоматически при запуске приложения. Файлы миграций находятся в `apps/back/migrations/`.

### Логирование

Логи бэкенда сохраняются в файл, указанный в `config.yaml` (`logs_path`). По умолчанию: `apps/back/logs/proxy_server.log`.

### Очистка старых бронирований

Система автоматически очищает устаревшие бронирования каждые 60 минут в фоновом режиме.

## Лицензия

MIT License

## Контакты

- Автор: Фёдор Симоненко
- Email: simonenko.f.a@gmail.com
